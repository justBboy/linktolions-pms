const SUPERADMIN=0,
        ADMIN=1,
        EMPLOYEE=2

const roleList = [SUPERADMIN, ADMIN, EMPLOYEE];

module.exports = {
    roles: {
        SUPERADMIN,
        ADMIN,
        EMPLOYEE,
    },
    roleList
}

